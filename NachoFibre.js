
class NachoFibre{

    fibonacci(n)
    {
        let num1 = 0;
        let num2 = 1;
        
        while (if n <= 0; = 0 {
            previous sum = num1 + num2;
            console.log num1
            num1 = num2;
            num2 = previous sum;
        
        })
        return -1;
        
    }

    kaleb(n){
        return -1;
    }

}

module.exports = NachoFibre;

// linear, sequential, most efficient way
// start with an array starting with 0,
// build the array by adding 1 to the starting number
// the result (1) is placed at the end of the array
// the result the number is equal to the sum of the 2 numbers that preceed it
// 