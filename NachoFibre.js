
class NachoFibre{

    fibonacci(n){
        // *fibonacci(0) = 0*
// * *fibonacci(1) = 1*
// * *fibonacci(n) = fibonacci(n-1) + fibonacci(n-2)*

// Given *n*, return the *nth* number in the sequence.
     let nth = []

     for (let i = 0; i < array.length; i++){
         if(n.length > n - 1 + n - 2);
     }



        return nth;
    }

    kaleb(n){
        let nth = []
        for(let i = 0; i < array.length; i++){
            if(n.length > 2 * n - 2 - n - 1);
        }

        return nth;
    }

}

module.exports = NachoFibre;