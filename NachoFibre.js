
class NachoFibre{

    fibonacci(n){
        // [0, 1, 1, 2, 3, 5, 8] so the 6th number is 8.
        //create an array that contains the first two entries
        let sequence = [0, 1, 1, 2, 3, 5, 8]
        let i = sequence[i]
        
        // create a fuction that will create/add to an array and calc
        let n = sequence.push([i-1] + [i-2])
       

        
        return n
    }

    kaleb(n){
        return -1;
    }

}

module.exports = NachoFibre;