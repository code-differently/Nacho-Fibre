
class NachoFibre{

    fibonacci(n) {
      //fibonacci(6) = fibonacci(5) + fibonacci(4)+ fibonacci(3)
      //check if n is 0 then the fibonnaci is 0 and if n is 1, then the fibonnaci is 1
      //store the fibonacci of each number in an array
      //iterate through the array, search for the index of n
      //return the number at that index
      let fibNum = [];
      let length = n;
      /*
      if(n == 0 || n == 1 )  
            fibNum.push(n);
      else
            fibNum.push(fibonacci(n - 1) + fibonacci(n - 2));
        
        console.log(fibNum); */
      for (let i = 0; i < n; i++){
          
      }
      
      
    }

    kaleb(n){
        return -1;
    }

}

module.exports = NachoFibre;